export enum PRODUCT_TYPE_ENUM {
  gift_card = 'gift_card',
  subscription = 'subscription',
}

export enum ORDER_STATUS_ENUM {
  pending = 'pending',
  completed = 'completed',
  rejected = 'rejected',
}
